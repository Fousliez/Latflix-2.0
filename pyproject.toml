[project]
name = "latflix-2"
version = "0.2.0"
description = "Fast clean rewrite of Latflix"
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.7,<7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
