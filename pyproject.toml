[project]
name = "latflix-2"
version = "2.1.0a1"
description = "Latflix 2.1 clean desktop rewrite"
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.7,<7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
